export const formatHeight = (heightDecimetres: number) =>
  `${(heightDecimetres * 0.1).toFixed(1)} m`;
export const formatWeight = (weightHectograms: number) =>
  `${(weightHectograms * 0.1).toFixed(1)} kg`;
